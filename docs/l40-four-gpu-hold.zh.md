# L40 四卡短暂占位记录（已关闭）

日期：2026-09-24。用户曾要求短暂占住 GPU 1、2、3、4；每卡用一个低计算驻留进程持有 40 GiB 显存。随后用户决定停止。**四个服务均已 `disable --now`，核验为 `inactive`/`disabled`，每卡占用恢复到约 3 MiB；服务器上的模板服务文件和驻留代码已删除。不存在后台占位。**GPU 0 的其他用户进程和 GPU 6 的训练任务未触碰。

该占位**不是在训练模型**，也不是调度器级 GPU 独占：其他进程仍可使用剩余显存及计算资源。其用途是让四张卡的大部分显存保持占用，而不空跑无价值计算。

- 仓库中仅保留历史实现 `scripts/l40_gpu_hold.py` 和 `configs/systemd/small-agent-l40-hold@.service`，不再部署或运行。服务器上的代码和服务文件均已移除。

本次占位没有训练模型，也不构成模型能力证据。未来训练直接使用空闲 GPU，不需要再启动占位服务。
